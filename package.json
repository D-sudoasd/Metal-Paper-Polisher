{
  "name": "metallic-materials-academic-editor",
  "version": "4.0.0",
  "private": true,
  "description": "Tooling to lint and validate the metallic-materials academic editing skill package.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-skill.mjs",
    "lint": "markdownlint-cli2 \"**/*.md\"",
    "test": "npm run validate && npm run lint"
  },
  "devDependencies": {
    "markdownlint-cli2": "0.14.0"
  }
}
